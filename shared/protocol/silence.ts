// 「보냈는데 아무 응답도 없다」 를 판정하는 시간 — 프로세스 둘이 같은 값을 봐야 한다.
//
// - main: `replyWatch` 가 이 시간 뒤 침묵 안내 말풍선을 낸다
// - renderer: `useOptimisticBusy` 의 낙관 「응답 중」 이 이 시간까지 버틴다
//
// 같은 값이어야 하는 이유(실측 2026-08-26): renderer 쪽이 짧았을 때(10초)
// 표시가 먼저 꺼지고 안내는 아직 안 온 10~30초 **침묵 구간**이 생겼다 —
// 사용자에게는 "느린 응답" 과 구별되지 않는 죽은 화면이다. 한쪽만 고치면
// 그 구간이 조용히 되살아나므로, 값은 여기 하나뿐이고 양쪽이 import 한다.
export const SILENCE_MS = 30_000
