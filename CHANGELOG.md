# Changelog

릴리스마다 `## [버전]` 절을 위에 쌓는다 — release 워크플로가 이 절을 릴리스 본문으로 추출한다.
버전을 올리고(`package.json`) 이 파일에 절을 더한 뒤 main 에 push 하면 배포가 나간다.
일상 작업은 `dev` 브랜치에 쌓고, 릴리스 때만 main 으로 머지한다.

## [0.1.4] - 2026-09-02

- **확장 「코드 지도」** — 프로젝트의 파일 사이 의존을 본문 탭에 그래프로 보여준다.
  모델을 부르지 않고 구문 분석(tree-sitter)만 쓰므로 같은 코드면 언제나 같은 그림이다.
  Java·Kotlin·TypeScript·TSX 를 읽고, **무엇을 읽었는지 지도 요약에 항상 적는다** —
  「파일 1개」가 지도가 깨진 것인지 읽을 것이 그것뿐이었는지 구분되게 했다.
  처음 열리는 자리는 직접 참조 수가 아니라 **영향 반경**(고치면 어디까지 번지나)으로 고른다
- 파일 트리에서 파일을 **우클릭**하면 설치된 확장의 명령이 맨 위에 뜬다 — 확장 패널을
  열어 두지 않아도, 그 확장을 고르지 않아도 나온다
- **이미지를 붙이면 실제로 모델에게 간다.** 이전에는 첨부해도 어댑터에서 조용히 버려졌다
- 파일 트리에서 **입력창으로 끌어다 놓으면** 그 경로가 글로 들어간다.
  창 밖에서 끌어온 파일은 화면 아무 데나 놓아도 첨부된다 (이전에는 입력창 바 위에서만)
- 파일 목록이 상한에 걸려 잘리면 **잘렸다고 알린다.** 이전에는 절반만 보여주면서
  전부인 것처럼 떴다 (디렉토리 5,895개짜리 프로젝트에서 55% 가 잘렸다)
- 오류가 사라지거나 JSON 째 뜨던 세 자리를 막았다
  - 다른 앱의 개발 서버를 우리 것으로 읽어 **남의 화면이 뜨던** 것
  - 게이트웨이 오류가 `{"error":{"message":…}}` 원문째 뜨던 것 — 이제 문장만 꺼낸다
    (모르는 모양이면 진단을 위해 원문을 남긴다)
  - 턴이 닫힌 뒤 온 오류가 **아무것도 안 남기고** 사라지던 것
- 사내 모델 재편(2026-08-28 공지) 반영 — `glm-5.2` 종료, 기본값은 `qwen3.8-27b`.
  등록된 모델은 컨텍스트 262K 이고 이미지를 받는다. 262K 를 넘기면 조용한 외부 폴백 없이
  즉시 실패한다 (프롬프트가 사외로 나가지 않는다)
- 내부: 300줄에 닿은 파일 넷을 추출로 풀고, davis WS 잔재를 접었다.
  개발 서버는 포트를 인자로 받는다 (`npm run dev -- 5274`)

## [0.1.3] - 2026-08-26

- 중지 버튼이 즉시 듣는다 — 응답이 시작되기 전(스피너만 도는 구간)에 눌러도 바로 풀린다.
  이전에는 그 구간의 중지가 최대 30초까지 무시된 것처럼 보였다
- 설정의 데스크톱 MCP 스위치가 창을 닫았다 연 뒤에도 제대로 먹는다 — 이전에는 첫 창의
  설정값이 굳어, 끄거나 켠 것이 실제 등록에 반영되지 않았다
- 내부: 침묵 판정 시간을 한 곳(`shared/protocol/silence.ts`)으로 모으고, 전송·진행 표시·중지
  배선을 잠그는 테스트를 세웠다. 못 도는 옛 테스트 3종은 접었다

## [0.1.2] - 2026-08-26

- 제품명을 **Closed Code Desktop** 으로 변경 (창 제목·앱 내 표기·패키징·산출물 파일명 — 이전: AXGentic Desktop)
- 산출물 파일명 변경: `closed-code-desktop-<arch>.zip` / `closed-code-desktop-x64.exe`
- 전송 즉시 진행 표시 — LLM 응답이 오기 전에도 「응답 중」 스피너가 바로 뜬다.
  응답이 없으면 30초까지 유지돼 침묵 안내 말풍선과 끊김 없이 이어진다
- 셸 결과를 대화로 넘길 때도 같은 진행 표시가 뜬다
- README 를 설치 안내 전용으로 간소화 (macOS·Windows) — 개발 문서는 `docs/DEVELOPMENT.md`

## [0.1.1] - 2026-08-25

- 제품명을 **AXGentic Desktop** 으로 통일 (창 제목·패키징·릴리스·산출물 파일명 — 이전: Open Code Desktop)
- 산출물 파일명 변경: `axgentic-desktop-<arch>.zip` / `axgentic-desktop-x64.exe`

## [0.1.0] - 2026-08-25

첫 공개 릴리스.

- opencode 실행 파일 동봉 — 폐쇄망에서 설치 파일 하나로 끝난다 (탐색: `OPENCODE_BIN` > 동봉 > PATH > 알려진 자리)
- 채팅·리소스 관리(@ 검색)·도구 승인·변경 검토(diff)·확장(테스트 시나리오 등)
- 커넥터(MCP) 다이얼로그 — 좌측 서버 리스트 · 우측 상세(원격 서버 도구 목록 실시간 조회)
- 셸 결과를 대화로 넘길 때 덧말 입력
- 동시 프로젝트 10개 · 프로젝트 간 확장 화면 격리
